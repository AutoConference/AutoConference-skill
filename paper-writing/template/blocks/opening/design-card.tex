% Opening figure: `design-card` (the former page-1 teaser; matches no real
% paper and is never drawn -- installed only when the operator forces it or
% the evidence cannot support a plot). The card itself is figures/hero.tex.
\IfFileExists{figures/hero.tex}{\input{figures/hero}}{}
