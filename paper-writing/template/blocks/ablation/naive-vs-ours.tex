% Ablation: `naive-vs-ours` (design-axes.md axis 9; P4).
% A two-level header, "Naive" and "Ours" over the same metrics, one row per
% configuration. Vertical rules between the two groups. \best bold.
% Caption ABOVE.
\begin{table}[t!]
  \caption{\textbf{Ablation of \slot{the core component}.} \slot{one
    sentence naming what Naive removes and what Ours adds}. Full metrics are
    in the appendix.}
  \label{tab:ablation}
  \vspace{-0.2cm}
  \centering
  {\fontsize{7.5pt}{9.5pt}\selectfont
   \begin{tabular}{l | c c | c c | c}
     \toprule
     \textbf{Configuration}
       & \multicolumn{2}{c|}{\textbf{Naive}} & \multicolumn{2}{c|}{\textbf{Ours}} & \\
       & \slot{m1}\up & \slot{m2}\up & \slot{m1}\up & \slot{m2}\up & \textbf{\slot{cost}} \\
     \midrule
     \slot{configuration A} & \slot{v} & \slot{v} & \best{\slot{v}} & \slot{v} & \slot{v} \\
     \slot{configuration B} & \slot{v} & \slot{v} & \slot{v} & \best{\slot{v}} & \slot{v} \\
     \bottomrule
   \end{tabular}}
\end{table}
