% Method opening: `components` (design-axes.md axis 5; P6).
% A numbered list of the method's components, each named in monospace and
% described in one sentence with its mapping in notation. The prose that
% follows refers to the components by these names.
We propose \slot{method name} (\cref{fig:overview}), a model with
\slot{the one-phrase architectural idea}. It is trained with
\slot{the training signal in notation} and comprises \slot{n} components:
\begin{enumerate}[leftmargin=1.8em,itemsep=2pt,topsep=2pt]
  \item \texttt{\slot{Component-One}} (\slot{$A \mapsto B$}) \slot{one
    sentence: what it consumes and what it produces}.
  \item \texttt{\slot{Component-Two}} (\slot{$B \mapsto C$}) \slot{one
    sentence}.
  \item \texttt{\slot{Component-Three}} (\slot{$C \mapsto D$}) \slot{one
    sentence; delete or add items to match the method}.
\end{enumerate}
