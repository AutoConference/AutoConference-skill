% Method opening: `preliminaries` (design-axes.md axis 5; P1, P4).
% The first subsection restates the prior framework the method builds on,
% in its own notation, before the method's own subsections begin. What is
% restated here is cited, not claimed.
\subsection{Preliminaries}
\label{sec:preliminaries}

\textbf{\slot{The prior framework, named}.}
\slot{two to four sentences restating the framework this method builds on,
with verified citations, in the notation the rest of the paper uses}
\slot{the framework's central object or objective, stated inline or as the
first display if the equation register allows}

\textbf{\slot{The specific mechanism the method modifies}.}
\slot{two to three sentences on the piece of the framework this paper
changes, and the sentence that says what is kept fixed}
