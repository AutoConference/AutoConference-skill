% Method opening: `formal` (design-axes.md axis 5; P5, P7).
% The problem stated formally, inline, before any subsection: what is given,
% what is produced, what the difficulty is. Notation introduced here is used
% unchanged for the rest of the paper.
Given \slot{the input, in notation: e.g. $X = \{X_i\}_{i=1}^{N}$ with its
meaning} and \slot{any side information}, the goal is to produce
\slot{the output, in notation} that \slot{the property the output must
have}. The key challenge is to \slot{the one sentence naming why this is
hard}. To this end, \slot{one sentence summarizing the method's answer,
naming its components and the subsections that present them}.
