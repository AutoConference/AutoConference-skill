% Method opening: `roadmap` (design-axes.md axis 5; P2).
% One paragraph that walks the reader through the subsections by number,
% then the subsections begin. Input at the top of 03_method.tex after the
% overview figure. Update the \cref targets to the method's actual labels.
In this section, we detail the methodology of \slot{method name}.
\Cref{sec:method-one} introduces \slot{the preliminaries and the overall
pipeline}. \Cref{sec:method-two} presents \slot{the core of the proposed
method}, followed by \cref{sec:method-three} introducing \slot{the remaining
component or the training strategy}.
