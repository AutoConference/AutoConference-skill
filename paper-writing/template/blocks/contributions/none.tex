% Contribution list: `none` (design-axes.md axis 3; P4).
% No list. The introduction states "Our key contribution is ..." inline in
% its last paragraph and does not use the environment. The macros still
% exist so a stray use compiles as plain prose rather than failing.
\newenvironment{contributionlist}{\par\noindent\ignorespaces}{\par}
\newcommand{\contribution}[1]{#1 }
