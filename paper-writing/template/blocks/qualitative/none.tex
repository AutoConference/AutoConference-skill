% Secondary figure: `none` (design-axes.md axis 10; P5, P6). The tables
% carry the evidence; no secondary figure is drawn. The experiments skeleton
% inputs this file inside an \IfFileExists{data/opening.dat} guard, so the
% narrating slot text around it must be deleted or rewritten by the writer.
