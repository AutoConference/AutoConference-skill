% Appendix: `single` (design-axes.md axis 11; P6). One appendix section.
% If the design also drew related = appendix, that full related-work section
% is appended after this file; if it drew limitations = appendix, the
% Limitations section renders here. Either way the appendix carries one
% substantive section of its own, so it is never an empty \appendix.
\appendix

\section{\slot{Additional Details}}
\label{sec:appendix-details}
\slot{the configuration, protocol and per-condition details the main body
pointed here, in one section of a few paragraphs and at most one table}

\LimitationsInAppendix
