% Experiments: `separate-ablation-section` (design-axes.md axis 7; P4).
% Experiments = Setup + Comparison with prior methods. The ablation study is
% its own top-level section (sections/04b_ablation.tex, installed alongside).
\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}
\label{sec:setup}

\textbf{Dataset and evaluation metrics.}
\slot{the benchmark, its size, what each metric measures and its direction}

\textbf{Implementation details.}
\slot{the default configuration and its parameters, hardware, and the fair-
comparison rule applied to every arm}

\subsection{Comparison with Prior Methods}
\label{sec:comparison}

\textbf{Quantitative Results.}
\input{tables/main_results}
In \cref{tab:main-results}, \slot{compare the primary arm against each group
of methods: where it leads, by how much, and the one trade-off it accepts}.

\textbf{\slot{Secondary Results}.}
\input{tables/secondary_results}
\slot{read the second table}
