% Layout `iclr1`: ICLR 2026 single column, official style file unmodified.
% The style file already loads natbib (author-year) and sets small-caps
% section headings; do not re-load natbib.
\newenvironment{acwidefigure}[1][t]{\begin{figure}[#1]}{\end{figure}}
\newenvironment{acwidetable}[1][t!]{\begin{table}[#1]}{\end{table}}
\newcommand{\acfloatwidth}{0.99\linewidth}
\newcommand{\acopeningwidth}{0.99\linewidth}
% ICLR's hyperref setup is the style's own; our preamble's colorlinks apply.
